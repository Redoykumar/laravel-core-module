Package Test Report

Files generated by PHPUnit for `Redoy/CoreModule` tests:

- `testdox.html` — human-readable TestDox HTML (summary of tests and success/failure states).
- `junit.xml` — JUnit-style XML report suitable for CI systems.

How to view locally:

1. Open `packages/Redoy/CoreModule/tests/report/testdox.html` in a browser.
2. Or use an XML viewer for `packages/Redoy/CoreModule/tests/report/junit.xml`.

Command used to generate these reports:

```
./vendor/bin/phpunit --log-junit packages/Redoy/CoreModule/tests/report/junit.xml \
  --testdox-html packages/Redoy/CoreModule/tests/report/testdox.html \
  packages/Redoy/CoreModule/tests
```

Notes:
- Tests passed: 35 tests, 129 assertions (no failures or errors) when generated.
- If you want the report hosted on a site, copy the `testdox.html` (and assets) into the package's public site directory.
